# Frontend Component

This directory is reserved for the frontend interface. The framework has not been chosen yet (options include Streamlit or React with TypeScript).
Once decided, the UI codebase will reside here.
